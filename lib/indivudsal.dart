import 'package:flutter/material.dart';

class indivudsal extends StatelessWidget {
  indivudsal({
    Key key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xffdfdfdf),
      body: Stack(
        children: <Widget>[],
      ),
    );
  }
}
